<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="DataBindingSample.BindingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:DataBindingSample"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

  <Page.Resources>
    <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    <local:BoolNegationConverter x:Key="BoolNegationConverter" />
    <local:ColorToBrushConverter x:Key="ColorToBrushConverter" />
    <local:BoolToGenderConverter x:Key="BoolToGenderConverter" />
    <local:ObjectNullConverter x:Key="ObjectNullConverter" />
    <local:ObjectNotNullConverter x:Key="ObjectNotNullConverter" />

    <local:SubViewModel x:Key="SubViewModel" />
  </Page.Resources>

  <Page.DataContext>
    <local:MainViewModel x:Name="ViewModel" />
  </Page.DataContext>

  <StackPanel Spacing="4">
    <!--#region Section 1: Binding Source 1 -->
    <Expander Style="{StaticResource View_ExpanderStyle}">
      <Expander.Header>
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <TextBlock Text="1. Binding Source: Text"
                     Grid.Column="0"
                     Style="{StaticResource SubtitleTextBlockStyle}" />
          <ToggleButton x:Name="View_S1_CodeToggleButton"
                        Grid.Column="1"
                        Content="Source code" />
        </Grid>
      </Expander.Header>

      <StackPanel Style="{StaticResource View_StackPanelStyle}">
        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve">텍스트 상자에 텍스트를 입력하세요. 입력된 값을 바탕으로 데이터를 바인딩합니다.</Span>
        </TextBlock>
        <Grid ColumnSpacing="8">
          <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
          </Grid.ColumnDefinitions>
          <TextBox x:Name="View_S1_MainInputTextBox"
                   Grid.Column="0"
                   Text="{Binding Main_S1_Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                   PlaceholderText="Enter main text." />
          <TextBox x:Name="View_S1_SubInputTextBox"
                   Grid.Column="1"
                   Text="{Binding Source={StaticResource SubViewModel}, Path=Sub_S1_Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                   PlaceholderText="Enter sub text." />
        </Grid>
        <local:CodeSample x:Name="View_S1_1stCodeSample"
                          Visibility="{Binding ElementName=View_S1_CodeToggleButton, Path=IsChecked}" />

        <Rectangle Style="{StaticResource View_SeparatorStyle}" />

        <TextBlock Text="DataContext"
                   Style="{StaticResource View_SubtitleTextBlockStyle}" />
        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve">* 바인딩 소스를 지정하지 않으면 Page.DataContext에 생성한 인스턴스를 사용합니다.
* DataContext에 내부 속성을 Path 바인딩 속성으로 사용할 수 있습니다.</Span>
        </TextBlock>
        <Border Style="{StaticResource View_BorderStyle}">
          <TextBlock Text="{Binding Main_S1_Text}" />
        </Border>
        <local:CodeSample x:Name="View_S1_2ndCodeSample"
                          Visibility="{Binding ElementName=View_S1_CodeToggleButton, Path=IsChecked}" />

        <Rectangle Style="{StaticResource View_SeparatorStyle}" />

        <TextBlock Text="Source"
                   Style="{StaticResource View_SubtitleTextBlockStyle}" />
        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve">* 바인딩 소스를 Source 바인딩 속성으로 하면, {StaticResource} 마크업 확장을 사용하여 Page.Resources 또는 앱 리소스(ResourceDictionary)에 정의한 인스턴스를 지정합니다.
* StaticResource로 지정한 인스턴스의 내부 속성을 Path 바인딩 속성으로 사용할 수 있습니다.</Span>
        </TextBlock>
        <Border Style="{StaticResource View_BorderStyle}">
          <TextBlock Text="{Binding Source={StaticResource SubViewModel}, Path=Sub_S1_Text}" />
        </Border>
        <local:CodeSample x:Name="View_S1_3rdCodeSample"
                          Visibility="{Binding ElementName=View_S1_CodeToggleButton, Path=IsChecked}" />

        <Rectangle Style="{StaticResource View_SeparatorStyle}" />

        <TextBlock Text="ElementName"
                   Style="{StaticResource View_SubtitleTextBlockStyle}" />
        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve">* 바인딩 소스를 ElementName 바인딩 속성으로 하면, x:Name 특성이 있는 동일한 XAML의 다른 UI 요소를 참조합니다.
* 해당 UI 요소의 내부 속성을 Path 바인딩 속성으로 사용할 수 있습니다.</Span>
        </TextBlock>
        <Border Style="{StaticResource View_BorderStyle}">
          <TextBlock Text="{Binding ElementName=View_S1_MainInputTextBox, Path=Text}" />
        </Border>
        <local:CodeSample x:Name="View_S1_4thCodeSample"
                          Visibility="{Binding ElementName=View_S1_CodeToggleButton, Path=IsChecked}" />
      </StackPanel>
    </Expander>

    <!--#endregion -->

    <Border Height="12" />

    <!--#region Section 2: Binding Source 2 -->
    <Expander Style="{StaticResource View_ExpanderStyle}">
      <Expander.Header>
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <TextBlock Text="2. Binding Source: Slider"
                     Grid.Column="0"
                     Style="{StaticResource SubtitleTextBlockStyle}" />
          <ToggleButton x:Name="View_S2_CodeToggleButton"
                        Grid.Column="1"
                        Content="Source code" />
        </Grid>
      </Expander.Header>

      <StackPanel Style="{StaticResource View_StackPanelStyle}">

        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve">슬라이더를 움직여 값을 조정하고 사각형의 크기를 변경하세요. 조정된 값을 바탕으로 데이터를 바인딩합니다.</Span>
        </TextBlock>

        <Slider x:Name="View_S2_SizeSlider"
                Minimum="50"
                Maximum="150"
                Width="400"
                Value="{Binding Main_S2_SliderValue, Mode=TwoWay}" />
        <local:CodeSample x:Name="View_S2_1stCodeSample"
                          Visibility="{Binding ElementName=View_S2_CodeToggleButton, Path=IsChecked}" />

        <Rectangle Style="{StaticResource View_SeparatorStyle}" />

        <TextBlock Text="DataContext"
                   Style="{StaticResource View_SubtitleTextBlockStyle}" />
        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve">* 바인딩 소스를 지정하지 않으면 Page.DataContext에 생성한 인스턴스를 사용합니다.
* DataContext에 내부 속성을 Path 바인딩 속성으로 사용할 수 있습니다.</Span>
        </TextBlock>
        <Border Background="LightPink"
                Width="{Binding Main_S2_SliderValue}"
                Height="{Binding Main_S2_SliderValue}">
          <TextBlock Text="{Binding Main_S2_SliderValue}"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center" />
        </Border>
        <local:CodeSample x:Name="View_S2_2ndCodeSample"
                          Visibility="{Binding ElementName=View_S2_CodeToggleButton, Path=IsChecked}" />

        <Rectangle Style="{StaticResource View_SeparatorStyle}" />

        <TextBlock Text="ElementName"
                   Style="{StaticResource View_SubtitleTextBlockStyle}" />
        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve">* 바인딩 소스를 ElementName 바인딩 속성으로 하면, x:Name 특성이 있는 동일한 XAML의 다른 UI 요소를 참조합니다.
* 해당 UI 요소의 내부 속성을 Path 바인딩 속성으로 사용할 수 있습니다.</Span>
        </TextBlock>
        <Border x:Name="View_S2_Border"
                Background="LightBlue"
                Width="{Binding ElementName=View_S2_SizeSlider, Path=Value}"
                Height="{Binding ElementName=View_S2_SizeSlider, Path=Value}">
          <TextBlock Text="{Binding ElementName=View_S2_Border, Path=Height}"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center" />
        </Border>
        <local:CodeSample x:Name="View_S2_3rdCodeSample"
                          Visibility="{Binding ElementName=View_S2_CodeToggleButton, Path=IsChecked}" />

        <Rectangle Style="{StaticResource View_SeparatorStyle}" />

        <TextBlock Text="RelativeSource Self"
                   Style="{StaticResource View_SubtitleTextBlockStyle}" />
        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve">* 바인딩 소스를 RelativeSource 바인딩 속성으로 하면, 바인딩 대상(Binding을 사용하는 현재 요소)의 위치를 기준으로 상대적으로 바인딩 소스를 지정합니다.
* {RelativeSource Self}는 바인딩 대상인 자기 자신을 바인딩 소스로 사용합니다. 자기 자신의 내부 속성을 Path 바인딩 속성으로 사용할 수 있습니다.</Span>
        </TextBlock>
        <Border Background="LightGreen"
                Width="{Binding ElementName=View_S2_SizeSlider, Path=Value}"
                Height="{Binding RelativeSource={RelativeSource Self}, Path=Width}">
          <TextBlock Text="{Binding Main_S2_SliderValue}"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center" />
        </Border>
        <local:CodeSample x:Name="View_S2_4thCodeSample"
                          Visibility="{Binding ElementName=View_S2_CodeToggleButton, Path=IsChecked}" />
      </StackPanel>
    </Expander>

    <!--#endregion -->

    <Border Height="12" />

    <!--#region Section 3: Path -->
    <Expander Style="{StaticResource View_ExpanderStyle}">
      <Expander.Header>
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <TextBlock Text="3. Path"
                     Grid.Column="0"
                     Style="{StaticResource SubtitleTextBlockStyle}" />
          <ToggleButton x:Name="View_S3_CodeToggleButton"
                        Grid.Column="1"
                        Content="Source code" />
        </Grid>
      </Expander.Header>

      <StackPanel Style="{StaticResource View_StackPanelStyle}">
        <TextBlock Text="Object"
                   Style="{StaticResource View_SubtitleTextBlockStyle}" />
        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve">텍스트 상자에 텍스트를 입력하세요. 값을 입력하면 뷰모델(MainViewModel) 속성에 반영합니다.</Span>
        </TextBlock>

        <TextBox x:Name="View_S3_TextBox"
                 PlaceholderText="Enter text."
                 TextChanged="View_S3_TextBox_TextChanged" />
        <local:CodeSample x:Name="View_S3_1stCodeSample"
                          Visibility="{Binding ElementName=View_S3_CodeToggleButton, Path=IsChecked}" />

        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve"><Run FontWeight="SemiBold">[ Property ]</Run>
* 객체의 일반 public 프로퍼티(속성)에 바인딩할 수 있습니다.
* 단, 속성 변경 시 변경 사항을 알리지 않기 때문에 UI에 반영되지 않습니다.
* 프로퍼티가 아닌 필드에는 바인딩할 수 없습니다.</Span>
        </TextBlock>
        <Border Style="{StaticResource View_BorderStyle}">
          <TextBlock Text="{Binding Main_S3_Text1}" />
        </Border>
        <local:CodeSample x:Name="View_S3_2ndCodeSample"
                          Visibility="{Binding ElementName=View_S3_CodeToggleButton, Path=IsChecked}" />

        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve"><Run FontWeight="SemiBold">[ Observable Object ]</Run>
* 프로퍼티가 포함된 클래스가 INotifyPropertyChanged 인터페이스를 구현하고, 프로퍼티 값 변경 시 INotifyPropertyChanged.PropertyChanged 이벤트를 발생시키면 UI에 변경을 알릴 수 있습니다.
* CommunityToolkit.Mvvm.ComponentModel NuGet 패키지를 설치하면, INotifyPropertyChanged를 구현한 ObservableObject 클래스를 제공합니다.
* ObservableObject를 상속하고, 속성 변경 시 setter에서 SetProperty 메서드를 실행시키면 UI에 변경 사항이 반영됩니다.</Span>
        </TextBlock>
        <Border Style="{StaticResource View_BorderStyle}">
          <TextBlock Text="{Binding Main_S3_Text2}" />
        </Border>
        <local:CodeSample x:Name="View_S3_3rdCodeSample"
                          Visibility="{Binding ElementName=View_S3_CodeToggleButton, Path=IsChecked}" />

        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve"><Run FontWeight="SemiBold">[ DependencyProperty ]</Run>
* DependencyProperty는 DependencyObject를 상속한 클래스에서 사용 가능한, 프레임워크 관리하는 특수한 유형의 프로퍼티입니다.
* 프로퍼티의 값 저장, 변경 감지, 데이터 바인딩 연결, 스타일, 애니메이션, 메타데이터 관리 등을 프레임워크 레벨에서 관리합니다.
* 프레임워크에서 자동으로 변경 알림을 전파하여 바인딩 대상에 전달하기 때문에 INotifyPropertyChanged를 구현하지 않아도 됩니다.
* 일반 XAML 컨트롤의 속성들은 대부분 DependencyProperty입니다.</Span>
        </TextBlock>
        <Border Style="{StaticResource View_BorderStyle}">
          <TextBlock Text="{Binding ElementName=View_S3_TextBox, Path=Text}" />
        </Border>
        <local:CodeSample x:Name="View_S3_4thCodeSample"
                          Visibility="{Binding ElementName=View_S3_CodeToggleButton, Path=IsChecked}" />

        <Rectangle Style="{StaticResource View_SeparatorStyle}" />

        <TextBlock Text="Collections"
                   Style="{StaticResource View_SubtitleTextBlockStyle}" />
        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve">컬렉션의 항목을 위치 또는 인덱스로 지정할 수 있습니다.</Span>
        </TextBlock>
        <TextBlock Text="[ Array ]"
                   FontWeight="SemiBold" />
        <Border Style="{StaticResource View_BorderStyle}">
          <TextBlock Text="{Binding Main_S3_Array[2]}" />
        </Border>
        <local:CodeSample x:Name="View_S3_5thCodeSample"
                          Visibility="{Binding ElementName=View_S3_CodeToggleButton, Path=IsChecked}" />

        <TextBlock Text="[ Jagged Array ]"
                   FontWeight="SemiBold" />
        <Border Style="{StaticResource View_BorderStyle}">
          <TextBlock Text="{Binding Main_S3_JaggedArray[1][2]}" />
        </Border>
        <local:CodeSample x:Name="View_S3_6thCodeSample"
                          Visibility="{Binding ElementName=View_S3_CodeToggleButton, Path=IsChecked}" />

        <TextBlock Text="[ List ]"
                   FontWeight="SemiBold" />
        <Border Style="{StaticResource View_BorderStyle}">
          <TextBlock Text="{Binding Main_S3_List[2]}" />
        </Border>
        <local:CodeSample x:Name="View_S3_7thCodeSample"
                          Visibility="{Binding ElementName=View_S3_CodeToggleButton, Path=IsChecked}" />
        <TextBlock Text="[ Dictionary ]"
                   FontWeight="SemiBold" />
        <Border Style="{StaticResource View_BorderStyle}">
          <TextBlock Text="{Binding Main_S3_Dictionary[Third]}" />
        </Border>
        <local:CodeSample x:Name="View_S3_8thCodeSample"
                          Visibility="{Binding ElementName=View_S3_CodeToggleButton, Path=IsChecked}" />

        <Rectangle Style="{StaticResource View_SeparatorStyle}" />

        <TextBlock Text="Sub-Properties / AttachedProperty"
                   Style="{StaticResource View_SubtitleTextBlockStyle}" />
        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve">슬라이더를 움직여 위치를 조정하세요.
* 프로퍼티의 연속적인 하위 프로퍼티를 참조할 수 있습니다.
* AttachedProperty를 참조할 때는 소유자와 소유자에 지정된 연결된 속성 이름을 괄호로 묶습니다.</Span>
        </TextBlock>
        <StackPanel Spacing="30"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center">
          <Slider Header="Position X"
                  Minimum="0"
                  Maximum="150"
                  Width="250"
                  Value="{Binding Main_S3_Dog.Position.X, Mode=TwoWay}" />
          <Slider Header="Position Y"
                  Minimum="0"
                  Maximum="150"
                  Width="250"
                  Value="{Binding Main_S3_Dog.Position.Y, Mode=TwoWay}" />
        </StackPanel>
        <Grid ColumnSpacing="24"
              HorizontalAlignment="Center">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>

          <Canvas Grid.Column="0"
                  Width="200"
                  Height="200"
                  Background="{StaticResource SolidBackgroundFillColorBase}">
            <TextBlock x:Name="View_S3_Dog"
                       Canvas.Left="{Binding Main_S3_Dog.Position.X}"
                       Canvas.Top="{Binding Main_S3_Dog.Position.Y}"
                       Text="&#x1F436;"
                       FontSize="32" />
          </Canvas>
          <StackPanel Grid.Column="1"
                      VerticalAlignment="Center"
                      Spacing="8">
            <TextBlock Text="Name: " />
            <TextBlock Text="Icon: " />
            <TextBlock Text="Position X: " />
            <TextBlock Text="Position Y: " />
          </StackPanel>
          <StackPanel Grid.Column="2"
                      VerticalAlignment="Center"
                      Spacing="8">
            <TextBlock Text="{Binding Main_S3_Dog.Name}" />
            <TextBlock Text="{Binding ElementName=View_S3_Dog, Path=Text}" />
            <TextBlock Text="{Binding ElementName=View_S3_Dog, Path=(Canvas.Left)}" />
            <TextBlock Text="{Binding ElementName=View_S3_Dog, Path=(Canvas.Top)}" />
          </StackPanel>
        </Grid>

        <local:CodeSample x:Name="View_S3_9thCodeSample"
                          Visibility="{Binding ElementName=View_S3_CodeToggleButton, Path=IsChecked}" />
      </StackPanel>
      <!--#endregion -->
    </Expander>

    <Border Height="12" />

    <!--#region Section 4: Mode -->
    <Expander Style="{StaticResource View_ExpanderStyle}">
      <Expander.Header>
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <TextBlock Text="4. Mode"
                     Grid.Column="0"
                     Style="{StaticResource SubtitleTextBlockStyle}" />
          <ToggleButton x:Name="View_S4_CodeToggleButton"
                        Grid.Column="1"
                        Content="Source code" />
        </Grid>
      </Expander.Header>

      <StackPanel Style="{StaticResource View_StackPanelStyle}">
        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve">바인딩의 방향을 지정합니다. {Binding} 마크업 확장의 기본 바인딩 모드는 OneWay입니다. 바인딩 모드를 명시하지 않으면 OneWay 모드로 동작합니다.

<Run FontWeight="SemiBold">[ OneTime ]</Run>
UI가 처음 표시될 때에만 값을 표시하고 값의 변경에는 반응하지 않습니다.

<Run FontWeight="SemiBold">[ OneWay ]</Run>
UI가 데이터 소스의 값을 관찰하고 값 변경 시 UI를 업데이트합니다.

<Run FontWeight="SemiBold">[ TwoWay ]</Run>
UI가 데이터 소스의 값을 관찰하고 값 변경 시 UI를 업데이트하며, 반대로 UI에서 값을 변경하면 변경 사항이 데이터 소스에 반영됩니다.</Span>
        </TextBlock>

        <Rectangle Style="{StaticResource View_SeparatorStyle}" />

        <StackPanel Orientation="Horizontal"
                    Spacing="24"
                    HorizontalAlignment="Center">
          <!-- OneTime -->
          <StackPanel Spacing="12"
                      Width="200">
            <TextBlock Text="OneTime"
                       Style="{StaticResource BodyStrongTextBlockStyle}"
                       HorizontalAlignment="Center" />
            <TextBox x:Name="View_S4_OneTimeSourceTextBox"
                     PlaceholderText="Enter text."
                     Text="OneTime" />
            <TextBox x:Name="View_S4_OneTimeTargetTextBox"
                     Text="{Binding ElementName=View_S4_OneTimeSourceTextBox, Path=Text, Mode=OneTime}" />
          </StackPanel>

          <!-- OneWay -->
          <StackPanel Spacing="12"
                      Width="200">
            <TextBlock Text="OneWay"
                       Style="{StaticResource BodyStrongTextBlockStyle}"
                       HorizontalAlignment="Center" />
            <TextBox x:Name="View_S4_OneWaySourceTextBox"
                     PlaceholderText="Enter text." />
            <TextBox x:Name="View_S4_OneWayTargetTextBox"
                     PlaceholderText="Mirrors above text."
                     Text="{Binding ElementName=View_S4_OneWaySourceTextBox, Path=Text, Mode=OneWay}" />
          </StackPanel>

          <!-- TwoWay -->
          <StackPanel Spacing="12"
                      Width="200">
            <TextBlock Text="TwoWay"
                       Style="{StaticResource BodyStrongTextBlockStyle}"
                       HorizontalAlignment="Center" />
            <TextBox x:Name="View_S4_TwoWaySourceTextBox"
                     PlaceholderText="Enter text."
                     Text="{Binding ElementName=View_S4_TwoWayTargetTextBox, Path=Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox x:Name="View_S4_TwoWayTargetTextBox"
                     PlaceholderText="Mirrors and edit above text."
                     Text="{Binding ElementName=View_S4_TwoWaySourceTextBox, Path=Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
          </StackPanel>
        </StackPanel>

        <local:CodeSample x:Name="View_S4_1stCodeSample"
                          Visibility="{Binding ElementName=View_S4_CodeToggleButton, Path=IsChecked}" />
      </StackPanel>
    </Expander>
    <!--#endregion -->

    <Border Height="12" />

    <!--#region Section 5: UpdateSourceTrigger -->
    <Expander Style="{StaticResource View_ExpanderStyle}">
      <Expander.Header>
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <TextBlock Text="5. UpdateSourceTrigger"
                     Grid.Column="0"
                     Style="{StaticResource SubtitleTextBlockStyle}" />
          <ToggleButton x:Name="View_S5_CodeToggleButton"
                        Grid.Column="1"
                        Content="Source code" />
        </Grid>
      </Expander.Header>
      <StackPanel Style="{StaticResource View_StackPanelStyle}">
        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve">TwoWay 바인딩 모드일 때 바인딩 소스를 업데이트할 타이밍을 지정합니다.
  
<Run FontWeight="SemiBold">[ Default ]</Run>
바인딩을 사용하는 DependencyProperty의 기본 동작을 사용합니다. LostFocus인 TextBlock.Text를 제외하고 대부분 PropertyChanged이 기본 동작입니다.
  
<Run FontWeight="SemiBold">[ LostFocus ]</Run>
바인딩 대상 컨트롤의 포커스가 해제될 때 바인딩 소스가 업데이트됩니다.
  
<Run FontWeight="SemiBold">[ PropertyChanged ]</Run>
바인딩 대상의 값이 변경될 때마다 바인딩 소스가 업데이트됩니다.
  
<Run FontWeight="SemiBold">[ Explicit ]</Run>
바인딩 대상에서 BindingExpression.UpdateSource 메서드를 호출할 때만 바인딩 소스가 업데이트됩니다. {Binding} 마크업 확장에서만 지원합니다.</Span>
        </TextBlock>

        <Rectangle Style="{StaticResource View_SeparatorStyle}" />

        <StackPanel Orientation="Horizontal"
                    Spacing="24"
                    HorizontalAlignment="Center">
          <!-- LostFocus -->
          <StackPanel Spacing="12"
                      Width="200">
            <TextBlock Text="LostFocus"
                       Style="{StaticResource BodyStrongTextBlockStyle}"
                       HorizontalAlignment="Center" />
            <TextBox PlaceholderText="LostFocus"
                     Text="{Binding Main_S5_Text1, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
            <Border Style="{StaticResource View_BorderStyle}">
              <TextBlock Text="{Binding Main_S5_Text1}" />
            </Border>
          </StackPanel>

          <!-- PropertyChanged -->
          <StackPanel Spacing="12"
                      Width="200">
            <TextBlock Text="PropertyChanged"
                       Style="{StaticResource BodyStrongTextBlockStyle}"
                       HorizontalAlignment="Center" />
            <TextBox PlaceholderText="PropertyChanged"
                     Text="{Binding Main_S5_Text2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Border Style="{StaticResource View_BorderStyle}">
              <TextBlock Text="{Binding Main_S5_Text2}" />
            </Border>
          </StackPanel>

          <!-- Explicit -->
          <StackPanel Spacing="12"
                      Width="200">
            <TextBlock Text="Explicit"
                       Style="{StaticResource BodyStrongTextBlockStyle}"
                       HorizontalAlignment="Center" />
            <TextBox x:Name="View_S5_ExplicitTextBox"
                     PlaceholderText="Explicit"
                     Text="{Binding Main_S5_Text3, Mode=TwoWay, UpdateSourceTrigger=Explicit}"
                     KeyUp="View_S5_ExplicitTextBox_KeyUp" />
            <Border Style="{StaticResource View_BorderStyle}">
              <TextBlock Text="{Binding Main_S5_Text3}" />
            </Border>
          </StackPanel>
        </StackPanel>

        <local:CodeSample x:Name="View_S5_1stCodeSample"
                          Visibility="{Binding ElementName=View_S5_CodeToggleButton, Path=IsChecked}" />
      </StackPanel>
    </Expander>
    <!--#endregion -->

    <Border Height="12" />

    <!--#region Section 6: Converter -->
    <Expander Style="{StaticResource View_ExpanderStyle}">
      <Expander.Header>
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <TextBlock Text="6. Converter"
                     Grid.Column="0"
                     Style="{StaticResource SubtitleTextBlockStyle}" />
          <ToggleButton x:Name="View_S6_CodeToggleButton"
                        Grid.Column="1"
                        Content="Source code" />
        </Grid>
      </Expander.Header>
      <StackPanel Style="{StaticResource View_StackPanelStyle}">
        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve">Converter는 바인딩 소스와 대상 간에 데이터를 변환할 수 있도록 하는 변환기입니다.
  
* Converter를 사용하려면 IValueConverter를 구현한 클래스를 만들어야 합니다.
* IValueConverter는 바인딩 소스와 대상 간의 데이터를 서로 변환하는 로직을 포함한 Convert 및 ConvertBack 메서드를 구현합니다. 
* Converter 인스턴스를 리소스에 생성하고 바인딩 시 {StaticResource} 마크업 확장으로 불러옵니다.</Span>
        </TextBlock>

        <Rectangle Style="{StaticResource View_SeparatorStyle}" />

        <TextBlock Style="{StaticResource View_SubtitleTextBlockStyle}"
                   Text="BoolToVisibilityConverter" />
        <ToggleSwitch x:Name="View_S6_FirstToggleSwitch" />
        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve">토글 스위치를 켜면 텍스트가 보입니다.
* ToggleSwitch.IsOn 속성의 bool 객체를 TextBlock.Visibility 속성의 Visibility 객체로 변환합니다.</Span>
        </TextBlock>
        <Border Style="{StaticResource View_BorderStyle}">
          <TextBlock Text="Visible"
                     Visibility="{Binding ElementName=View_S6_FirstToggleSwitch, Path=IsOn, Converter={StaticResource BoolToVisibilityConverter}}" />
        </Border>
        <local:CodeSample x:Name="View_S6_1stCodeSample"
                          Visibility="{Binding ElementName=View_S6_CodeToggleButton, Path=IsChecked}" />

        <Rectangle Style="{StaticResource View_SeparatorStyle}" />

        <TextBlock Style="{StaticResource View_SubtitleTextBlockStyle}"
                   Text="BoolNegationConverter (ConvertBack)" />
        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve">위 토글 스위와 반대로 동작합니다.
* ToggleSwitch.IsOn 속성의 bool 값을 반전시킵니다.
* TwoWay 바인딩 시 IValueConverter.ConverBack 메서드를 구현해야 합니다.</Span>
        </TextBlock>
        <ToggleSwitch x:Name="View_S6_SecondToggleSwitch"
                      IsOn="{Binding ElementName=View_S6_FirstToggleSwitch, Path=IsOn, Mode=TwoWay, Converter={StaticResource BoolNegationConverter}}" />
        <local:CodeSample x:Name="View_S6_2ndCodeSample"
                          Visibility="{Binding ElementName=View_S6_CodeToggleButton, Path=IsChecked}" />

        <Rectangle Style="{StaticResource View_SeparatorStyle}" />

        <TextBlock Style="{StaticResource View_SubtitleTextBlockStyle}"
                   Text="ColorToBrushConverter" />
        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve">색상 선택기에서 색상을 선택하세요.
* ColorPicker.Color 속성의 Color 객체를 Rectangle.Fill 속성의 Brush 객체로 변환합니다.</Span>
        </TextBlock>
        <Rectangle Stretch="Fill"
                   Height="50"
                   RadiusX="24"
                   RadiusY="24"
                   Fill="{Binding ElementName=View_S6_ColorPicker, Path=Color, Mode=OneWay, Converter={StaticResource ColorToBrushConverter}}" />
        <ColorPicker x:Name="View_S6_ColorPicker"
                     Color="LightGoldenrodYellow"
                     ColorSpectrumShape="Ring"
                     IsMoreButtonVisible="True"
                     IsColorPreviewVisible="False"
                     HorizontalAlignment="Center" />
        <local:CodeSample x:Name="View_S6_3rdCodeSample"
                          Visibility="{Binding ElementName=View_S6_CodeToggleButton, Path=IsChecked}" />
      </StackPanel>
    </Expander>
    <!--#endregion -->

    <Border Height="12" />

    <!--#region Section 7: FallbackValue / TargetNullValue -->
    <Expander Style="{StaticResource View_ExpanderStyle}">
      <Expander.Header>
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <TextBlock Text="7. FallbackValue / TargetNullValue"
                     Grid.Column="0"
                     Style="{StaticResource SubtitleTextBlockStyle}" />
          <ToggleButton x:Name="View_S7_CodeToggleButton"
                        Grid.Column="1"
                        Content="Source code" />
        </Grid>
      </Expander.Header>

      <StackPanel Style="{StaticResource View_StackPanelStyle}">
        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve"><Run FontWeight="SemiBold">[ FallbackValue ]</Run>
바인딩 소스 또는 바인딩 경로를 확인할 수 없는 경우(바인딩 실패 시) 기본적으로 표시할 값을 지정합니다.
  
<Run FontWeight="SemiBold">[ TargetNullValue ]</Run>
바인딩 소스 값이 명시적으로 null인 경우 대신 표시할 값을 지정합니다.

* {Binding Property.Name}과 같이 하위 속성(Name)을 참조할 때, 상위 속성(Property)이 null이면 TargetNullValue가 아닌 FallbackValue가 표시됩니다.</Span>
        </TextBlock>

        <Rectangle Style="{StaticResource View_SeparatorStyle}" />

        <StackPanel Orientation="Horizontal"
                    Spacing="24">
          <Border Style="{StaticResource View_BorderStyle}"
                  Width="400">
            <StackPanel Orientation="Horizontal"
                        Spacing="24">
              <StackPanel Spacing="8">
                <TextBlock Text="Object Type:" />
                <TextBlock Text="Name:" />
                <TextBlock Text="Gender:" />
                <TextBlock Text="Id:" />
                <TextBlock Text="Description:" />
              </StackPanel>

              <StackPanel Spacing="8">
                <TextBlock Text="{Binding Main_S7_Dog, FallbackValue='Cannot be resolved (FallbackValue)', TargetNullValue='Target is null (TargetNullValue)'}" />
                <TextBlock Text="{Binding Main_S7_Dog.Name, FallbackValue='Cannot be resolved (FallbackValue)', TargetNullValue='Target is null (TargetNullValue)'}" />
                <TextBlock Text="{Binding Main_S7_Dog.Gender, Converter={StaticResource BoolToGenderConverter}, FallbackValue='Cannot be resolved (FallbackValue)', TargetNullValue='Target is null (TargetNullValue)'}" />
                <TextBlock Text="{Binding Main_S7_Dog.Id, FallbackValue='Cannot be resolved (FallbackValue)', TargetNullValue='Target is null (TargetNullValue)'}" />
                <TextBlock Text="{Binding Main_S7_Dog.Description, FallbackValue='Cannot be resolved (FallbackValue)', TargetNullValue='Target is null (TargetNullValue)'}" />
              </StackPanel>
            </StackPanel>
          </Border>

          <StackPanel Spacing="8"
                      VerticalAlignment="Center">
            <Button x:Name="View_S7_CreateButton"
                    Content="Create instance"
                    IsEnabled="{Binding Main_S7_Dog, Converter={StaticResource ObjectNullConverter}}"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="View_S7_CreateButton_Click" />
            <Button x:Name="View_S7_NullButton"
                    Content="Make object null"
                    IsEnabled="{Binding Main_S7_Dog, Converter={StaticResource ObjectNotNullConverter}}"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="View_S7_NullButton_Click" />
            <AutoSuggestBox x:Name="View_S7_AutoSuggestBox"
                            PlaceholderText="Enter description."
                            QueryIcon="Forward"
                            Width="200"
                            IsEnabled="{Binding Main_S7_Dog, Converter={StaticResource ObjectNotNullConverter}}"
                            QuerySubmitted="View_S7_AutoSuggestBox_QuerySubmitted" />
          </StackPanel>
        </StackPanel>

        <local:CodeSample x:Name="View_S7_1stCodeSample"
                          Visibility="{Binding ElementName=View_S7_CodeToggleButton, Path=IsChecked}" />
      </StackPanel>
    </Expander>
    <!--#endregion -->

    <Border Height="12" />

    <!--#region Section 8: ObservableCollection -->
    <Expander Style="{StaticResource View_ExpanderStyle}">
      <Expander.Header>
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <TextBlock Text="8. ObservableCollection"
                     Grid.Column="0"
                     Style="{StaticResource SubtitleTextBlockStyle}" />
          <ToggleButton x:Name="View_S8_CodeToggleButton"
                        Grid.Column="1"
                        Content="Source code" />
        </Grid>
      </Expander.Header>

      <StackPanel Style="{StaticResource View_StackPanelStyle}">
        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve">ObservableCollection은 항목의 추가, 제거, 이동, 새로 고침 등의 변경 사항이 발생했을 때, UI에 변경 알림을 제공하는 동적 데이터 컬렉션입니다.

* INotifyCollectionChanged을 상속하여 CollectionChanged 이벤트를 통해 컬렉션 항목 변경 시 알림을 제공합니다. 
* ListView와 같은 컬렉션 컨트롤에 동적인 컬렉션 항목 변경을 반영하려면, ItemSource로 일반 컬렉션(List 등)이 아닌 INotifyCollectionChanged를 구현한 컬렉션(ObservableCollection 등)을 지정해야 합니다.  
* ObservableCollection을 바인딩할 때, 컬렉션 인스턴스 자체의 교체가 없고 컬렉션 항목 변경만 알리고 싶은 경우 OneWay가 아닌 OneTime 바인딩 모드로도 충분합니다.</Span>
        </TextBlock>

        <Rectangle Style="{StaticResource View_SeparatorStyle}" />

        <ListView x:Name="View_S8_ListView"
                  BorderBrush="{StaticResource SurfaceStrokeColorDefault}"
                  BorderThickness="1"
                  CornerRadius="{StaticResource ControlCornerRadius}">
          <ListView.Header>
            <StackPanel Orientation="Horizontal"
                        Spacing="8"
                        Padding="16,8"
                        BorderBrush="{StaticResource SurfaceStrokeColorDefault}"
                        BorderThickness="0,0,0,1">
              <TextBlock Text="Type"
                         Width="80"
                         Style="{StaticResource BodyStrongTextBlockStyle}" />
              <TextBlock Text="Name"
                         Width="100"
                         Style="{StaticResource BodyStrongTextBlockStyle}" />
              <TextBlock Text="Gender"
                         Width="100"
                         Style="{StaticResource BodyStrongTextBlockStyle}" />
              <TextBlock Text="Id"
                         Width="100"
                         Style="{StaticResource BodyStrongTextBlockStyle}" />
              <TextBlock Text="Description"
                         Style="{StaticResource BodyStrongTextBlockStyle}" />
            </StackPanel>
          </ListView.Header>

          <ListView.ItemTemplate>
            <DataTemplate>
              <StackPanel Orientation="Horizontal"
                          Spacing="8">
                <TextBlock Text="{Binding}"
                           Width="80" />
                <TextBlock Text="{Binding Name}"
                           Width="100" />
                <TextBlock Text="{Binding Gender, Converter={StaticResource BoolToGenderConverter}}"
                           Width="100" />
                <TextBlock Text="{Binding Id}"
                           Width="100" />
                <TextBlock Text="{Binding Description, TargetNullValue='No description'}" />
              </StackPanel>
            </DataTemplate>
          </ListView.ItemTemplate>
        </ListView>

        <local:CodeSample x:Name="View_S8_1stCodeSample"
                          Visibility="{Binding ElementName=View_S8_CodeToggleButton, Path=IsChecked}" />

        <Grid ColumnSpacing="12">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
          </Grid.ColumnDefinitions>
          <Border Grid.Column="0"
                  Style="{StaticResource View_BorderStyle}">
            <RadioButtons SelectedIndex="0"
                          MaxColumns="1"
                          VerticalAlignment="Center">
              <RadioButtons.Header>
                <TextBlock Text="ItemsSource"
                           Style="{StaticResource BodyStrongTextBlockStyle}" />
              </RadioButtons.Header>
              <RadioButton x:Name="View_S8_DogsRadioButton"
                           Content="Dogs"
                           Checked="View_S8_DogsRadioButton_Checked" />
              <RadioButton x:Name="View_S8_CatsRadioButton"
                           Content="Cats"
                           Checked="View_S8_CatsRadioButton_Checked" />
            </RadioButtons>

          </Border>

          <Border Grid.Column="1"
                  Style="{StaticResource View_BorderStyle}">
            <StackPanel Spacing="8"
                        VerticalAlignment="Center">
              <TextBlock Text="Items"
                         Style="{StaticResource BodyStrongTextBlockStyle}" />
              <Button x:Name="View_S8_AddButton"
                      Content="Add a new item"
                      Background="{StaticResource AcrylicBackgroundFillColorBaseBrush}"
                      Click="View_S8_AddButton_Click" />
              <Button x:Name="View_S8_DeleteButton"
                      Content="Delete selected item"
                      IsEnabled="{Binding ElementName=View_S8_ListView, Path=SelectedItem, Converter={StaticResource ObjectNotNullConverter}}"
                      Background="{StaticResource AcrylicBackgroundFillColorBaseBrush}"
                      Click="View_S8_DeleteButton_Click" />
            </StackPanel>
          </Border>

          <Border Grid.Column="2"
                  Style="{StaticResource View_BorderStyle}">
            <TextBox Text="{Binding ElementName=View_S8_ListView, Path=SelectedItem.Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     IsEnabled="{Binding ElementName=View_S8_ListView, Path=SelectedItem, Converter={StaticResource ObjectNotNullConverter}}">
              <TextBox.Header>
                <TextBlock Text="Edit description"
                           Style="{StaticResource BodyStrongTextBlockStyle}" />
              </TextBox.Header>
            </TextBox>
          </Border>
        </Grid>
        <local:CodeSample x:Name="View_S8_2ndCodeSample"
                          Visibility="{Binding ElementName=View_S8_CodeToggleButton, Path=IsChecked}" />

        <TextBlock Style="{StaticResource View_DescriptionTextBlockStyle}">
<Span xml:space="preserve">
* {Binding} 마크업 확장은 런타임 시 바인딩을 결정하기 때문에 컴파일러는 바인딩 소스의 타입을 알 수 없습니다.
* 따라서 위 예시의 ListView.ItemTemplate 속성으로 DataTemplate을 지정할 때 x:DataType을 지정하지 않아도 Binding이 동작하며,
  ItemsSource의 컬렉션 항목 타입이 Dog, Cat 등으로 달라져도 동적으로 바인딩 경로를 찾습니다.
* Dog와 Cat의 Id 속성이 각각 int와 string으로 달라도 속성 이름이 같기 때문에 {Binding}에서는 동작합니다.
* 바인딩에서 타입 안정성보다 유연성이 중요할 때 {Binding}을 고려할 수 있습니다.</Span>
        </TextBlock>
      </StackPanel>
    </Expander>
    <!--#endregion -->

    <Border Height="12" />

    <!--#region Section 9: ControlTemplate -->
    <Expander Style="{StaticResource View_ExpanderStyle}">
      <Expander.Header>
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <TextBlock Text="9. ControlTemplate"
                     Grid.Column="0"
                     Style="{StaticResource SubtitleTextBlockStyle}" />
          <ToggleButton x:Name="View_S9_CodeToggleButton"
                        Grid.Column="1"
                        Content="Source code" />
        </Grid>
      </Expander.Header>

      <StackPanel Style="{StaticResource View_StackPanelStyle}">
        <TextBlock TextWrapping="Wrap"
                   IsTextSelectionEnabled="True">
<Span xml:space="preserve">컨트롤의 템플릿을 사용자 정의 ControlTemplate을 만들어 지정합니다.
  
* 컨트롤은 Microsoft.UI.Xaml.Controls 네임스페이스의 Control 클래스에서 파생된 XAML UI 요소를 의미하며, Template 속성으로 ControlTemplate을 지정할 수 있습니다.
* ControlTemplate 내부에서 이 템플릿을 기반으로 하는 컨트롤의 속성 값을 참조하는 방법은 두 가지가 있습니다.

<Run FontWeight="SemiBold">[ TemplateBinding ]</Run>
* 템플릿 시나리오에 대한 Binding의 최적화된 형태이며, 템플릿 기반 컨트롤의 속성 값을 ControlTemplate 내부 속성에 매핑합니다.
* 컨트롤 속성 값을 그대로 사용하는 간편하고 가벼운 방법입니다. Converter, Mode 등의 Binding 기능을 사용할 수 없습니다.
* TemplateBinding은 {Binding RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}를 사용하여 구성된 Binding과 유사합니다.
  
<Run FontWeight="SemiBold">[ RalativeSource TemplatedParent ]</Run>
* Converter, Mode 등의 Binding 기능이 필요할 때 사용합니다.
* TemplateBinding보다 무겁지만 컨트롤 속성 값에 접근하여 가공해서 쓰거나 다른 Binding 기능도 활용할 수 있습니다.</Span>
        </TextBlock>

        <Rectangle Style="{StaticResource View_SeparatorStyle}" />

        <TextBlock Text="ToggleButton"
                   Style="{StaticResource View_SubtitleTextBlockStyle}" />
        <ToggleButton Content="Click"
                      Background="{StaticResource AcrylicBackgroundFillColorBaseBrush}">
          <ToggleButton.Template>
            <ControlTemplate TargetType="ToggleButton">
              <Grid x:Name="RootGrid"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="{TemplateBinding CornerRadius}"
                    Padding="{TemplateBinding Padding}">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <!-- 토글 상태 표시 테두리 -->
                <Ellipse Grid.Column="0"
                         Width="8"
                         Height="8"
                         Margin="0,0,6,0"
                         Stroke="{StaticResource ControlStrongStrokeColorDefault}"
                         StrokeThickness="1" />

                <!-- 토글 상태 표시 내부 (토글이 켜지면 색상이 채워짐)  -->
                <Ellipse Grid.Column="0"
                         Width="8"
                         Height="8"
                         Margin="0,0,6,0"
                         Fill="{StaticResource AccentFillColorDefaultBrush}"
                         Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsChecked, Converter={StaticResource BoolToVisibilityConverter}}" />

                <!-- 콘텐츠 표시 부분 -->
                <ContentPresenter Grid.Column="1"
                                  Content="{TemplateBinding Content}" />

                <VisualStateManager.VisualStateGroups>
                  <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="PointerOver">
                      <VisualState.Setters>
                        <Setter Target="RootGrid.Background"
                                Value="{StaticResource ButtonBackgroundPointerOver}" />
                      </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                      <VisualState.Setters>
                        <Setter Target="RootGrid.Background"
                                Value="{StaticResource ButtonBackgroundPressed}" />
                      </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                      <VisualState.Setters>
                        <Setter Target="RootGrid.Background"
                                Value="{StaticResource ButtonBackgroundDisabled}" />
                      </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Checked" />
                    <VisualState x:Name="CheckedPointerOver">
                      <VisualState.Setters>
                        <Setter Target="RootGrid.Background"
                                Value="{StaticResource ButtonBackgroundPointerOver}" />
                      </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="CheckedPressed">
                      <VisualState.Setters>
                        <Setter Target="RootGrid.Background"
                                Value="{StaticResource ButtonBackgroundPressed}" />
                      </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="CheckedDisabled">
                      <VisualState.Setters>
                        <Setter Target="RootGrid.Background"
                                Value="{StaticResource ButtonBackgroundDisabled}" />
                      </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Indeterminate" />
                    <VisualState x:Name="IndeterminatePointerOver">
                      <VisualState.Setters>
                        <Setter Target="RootGrid.Background"
                                Value="{StaticResource ButtonBackgroundPointerOver}" />
                      </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="IndeterminatePressed">
                      <VisualState.Setters>
                        <Setter Target="RootGrid.Background"
                                Value="{StaticResource ButtonBackgroundPressed}" />
                      </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="IndeterminateDisabled">
                      <VisualState.Setters>
                        <Setter Target="RootGrid.Background"
                                Value="{StaticResource ButtonBackgroundDisabled}" />
                      </VisualState.Setters>
                    </VisualState>
                  </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>
              </Grid>
            </ControlTemplate>
          </ToggleButton.Template>
        </ToggleButton>
        <local:CodeSample x:Name="View_S9_1stCodeSample"
                          Visibility="{Binding ElementName=View_S9_CodeToggleButton, Path=IsChecked}" />
      </StackPanel>
    </Expander>
    <!--#endregion -->
  </StackPanel>
</Page>
