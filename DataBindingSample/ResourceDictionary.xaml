<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="using:DataBindingSample">
  <Style x:Key="View_ExpanderStyle"
         TargetType="Expander">
    <Setter Property="HorizontalAlignment"
            Value="Stretch" />
    <Setter Property="HorizontalContentAlignment"
            Value="Stretch" />
    <Setter Property="IsExpanded"
            Value="False" />
    <Setter Property="Background"
            Value="{StaticResource ControlFillColorDefault}" />
    <Setter Property="MaxWidth"
            Value="1200" />
  </Style>

  <Style x:Key="View_StackPanelStyle"
         TargetType="StackPanel">
    <Setter Property="Background"
            Value="{StaticResource ControlFillColorDefault}" />
    <Setter Property="Spacing"
            Value="12" />
  </Style>

  <Style x:Key="View_SubtitleTextBlockStyle"
         TargetType="TextBlock">
    <Setter Property="FontSize"
            Value="16" />
    <Setter Property="FontWeight"
            Value="SemiBold" />
    <Setter Property="Foreground"
            Value="{StaticResource AccentTextFillColorTertiaryBrush}" />
    <Setter Property="TextDecorations"
            Value="Underline" />
  </Style>

  <Style x:Key="View_DescriptionTextBlockStyle"
         TargetType="TextBlock">
    <Setter Property="IsTextSelectionEnabled"
            Value="True" />
    <Setter Property="TextWrapping"
            Value="Wrap" />
  </Style>

  <Style x:Key="View_BorderStyle"
         TargetType="Border">
    <Setter Property="MinHeight"
            Value="36" />
    <Setter Property="BorderBrush"
            Value="{StaticResource SurfaceStrokeColorDefault}" />
    <Setter Property="BorderThickness"
            Value="1" />
    <Setter Property="CornerRadius"
            Value="{StaticResource ControlCornerRadius}" />
    <Setter Property="Padding"
            Value="12,6" />
  </Style>

  <Style x:Key="View_SeparatorStyle"
         TargetType="Rectangle">
    <Setter Property="Height"
            Value="1" />
    <Setter Property="Fill"
            Value="{StaticResource ControlStrokeColorDefault}" />
    <Setter Property="Stretch"
            Value="Fill" />
    <Setter Property="Margin"
            Value="0,4" />
  </Style>
</ResourceDictionary>
